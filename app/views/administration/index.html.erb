<div class="page-header">
  <h1>Console d'administration</h1>
</div>
<table class="table table-striped table-bordered table-condensed">
  <thead>
    <tr>
      <td>Nom</td>
      <td># Contrats</td>
      <td># Fichiers</td>
      <td>Espace disque</td>
      <td>Dernière Connexion</td>
      <th class="right"><i class="icon-cog valign-text-bottom"></i> Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td>
          <%= ((user.firstname && user.lastname).nil?  ? user.email : "#{user.firstname.capitalize} #{user.lastname.upcase}") %>
        </td>
        <td><%=  user.contracts.size %></td>
        <td><%=  user.file_number %></td>
        <td></td>
        <td></td>
        <td>
          <a href="#">Voir</a>
          <a href="#">Modifier</a>
          <a href="#">Supprimer</a>
        </td>
      </tr>
    <% end -%>
  </tbody>
</table>
<%#= link_to 'Delete', user_contract_path(@user, contract), :method => :delete, :confirm => 'Are you sure?', :class => 'btn btn-mini btn-danger pull-right' %>
