<!DOCTYPE html>
<html>
  <head>
    <title>CmsAlphedys</title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>
  <body>
    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="#">Alphedys Conseil</a>
          <div class="nav-collapse">
            <% if current_user && current_user.is_admin? %>
            <ul class="nav">
              <li><%= link_to "Administration", administration_users_path %></li>
            </ul>
            <% end -%>
            <ul class="nav pull-right">
              <form class="navbar-search pull-left">
                <input type="text" class="search-query" placeholder="Rechercher">
              </form>
              <% if current_user %>
                <ul class="nav">
                  <li class="dropdown">
                    <a href="#"
                       class="dropdown-toggle"
                       data-toggle="dropdown">
                    <%= ((current_user.firstname && current_user.lastname).nil?  ? current_user.email :
                      "#{current_user.firstname.capitalize} #{current_user.lastname.upcase}") %>
                       <b class="caret"></b>
                     </a>

                    <%#=link_to content_tag('b', '', :class => 'caret') + "BOB",
                                current_user, :class => "dropdown-toggle", "data-toggle" => "dropdown"
                      %>
                    <ul class="dropdown-menu">
                      <li><%= link_to "Voir mon Profil", user_path(current_user) %></li>
                      <li><%= link_to "Editer mon Profil", edit_user_path(current_user) %></li>
                      <li class="divider"></li>
                      <li><%= link_to "Deconnexion", logout_path %></li>
                    </ul>
                  </li>
                </ul>
              <% else %>
                <!--<li><%#= link_to "Sign Up", signup_path %></li>-->
                <li><%= link_to "Connexion", login_path %></li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    </div>

    <div class="container">
      <div class="row">
        <% if content_for?(:breadcrumbs) %>
          <div class="span8 offset2">
            <%= yield(:breadcrumbs)  %>
          </div>
        <% end -%>
        <% flash.each do |name, msg| %>
          <div class="span8 offset2">
            <div class="alert alert-<%= name == :notice ? "success" : "error" %>">
              <a class="close" data-dismiss="alert">&times;</a>
              <%= msg %>
            </div>
          </div>
        <% end %>
        <%=  %>
        <div class="span<%=  session[:span].nil? ? "6" : session[:span] %> offset2">
          <%= yield %>
        </div>
        <% about = '<div class="span2"><div class="page-header"><h2>A propos</h2></div>Lorem ipsum dolor sit amet, consectetur adipisicing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.</div>' %>
        <%= yield(:right_pane) if content_for?(:right_pane) %>
      </div>
    </div>
  </body>
</html>
